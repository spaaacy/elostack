export const dynamic = "force-dynamic";
import AccountSettings from "@/components/account-settings/AccountSettings";

export const metadata = {
  title: "Account Settings | EloStack",
};

const Page = () => {
  return <AccountSettings />;
};

export default Page;
